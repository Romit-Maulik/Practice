### In progress

Prototypical deployment of CNN for Z500 emulation and subsequent variational data assimilation. 
- Uses periodic padding. 
- Uses resnet type forward model. 
- scipy minimize for DA (currently working on residual function specification)