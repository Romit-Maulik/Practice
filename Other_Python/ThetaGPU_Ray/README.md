# Parallelizing multiple LSTM trainings on ThetaGPU using Ray

To run - you need data from AIAEDA project and a `results/` folder. Its quite simple - look at the error messages.

```
ssh thetagpusn1
qsub submit_exp.sh
```